name: Release
on:
    create:
        branches: master
     workflow_dispatch:
     
jobs:
  build:
    name: Create Release
    runs-on: ubuntu-latest
    steps:
      - name: Clone Repo
        uses: actions/checkout@v3
        
