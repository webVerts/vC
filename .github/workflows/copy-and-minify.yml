name: Copy filters.js
on:
  workflow_dispatch:
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Copy filters.js
        run: |
          cp https://github.com/authifyWeb/filters/blob/main/URL/filters.js https://github.com/webVerts/wSource/tree/main/fame/filters.js
